import { SPINNER_SIZE, SpinnerColor } from "./types";
type Props = {
    color?: SpinnerColor;
    size?: (typeof SPINNER_SIZE)[number];
};
export declare const VuiSpinner: ({ color, size }: Props) => JSX.Element;
export {};
